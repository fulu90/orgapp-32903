<%# チャットヘッダー %>
<div class="chat-header">
  <div class="left-header">
    <div class="team-title">
      <%= @room.tname %>
    </div>
  </div>
  <div class="right-header">
    <div class="end-button">
      <%= link_to 'チャット終了', root_path %>
      <%= link_to '削除', room_path(@room), method: :delete %>
    </div>
  </div>
</div>
<%# チャットヘッダー %>

<div class="chat-main">
  <%# チャットメッセージ %>
  <div class="messages">
    <%= render partial: 'comment', collection: @comments %>
  </div>
  <%# チャットメッセージ %>
  
  <%# メモ %>
  <%# <% if current_user.admin? %> 
    <%# <%=form_with model: @memo, url: room_memos_path, local: true do |m| %> 
      <%# <div class="memo"> %>
          <%# <%= m.text_area :mtext, class: 'memo-text', placeholder: "memo" %> 
      <%# </div> %>
      <%# <%= m.submit '保存', class: 'memo-action-btn' %> 
    <%# <% end %> 
  <%# <% end %> 
  <%# メモ %>
</div>

<%# 送信フォーム %>
<%= form_with model: [@room, @comment], class: 'form', local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :content, class: 'form-message', placeholder: 'type a message' %>
  </div>
  <%= f.submit '送信', class: 'form-submit' %>
<% end %>
<%# 送信フォーム %>